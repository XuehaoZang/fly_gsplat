[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "fly_gsplat"
version = "0.1.0"

[project.entry-points."nerfstudio.method_configs"]
splatfacto-checkpoint = "models.splatfacto_checkpoint:splatfacto_checkpoint_method"

[tool.setuptools.packages.find]
include = ["models*"]